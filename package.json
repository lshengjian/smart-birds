{
  "name": "smart-birds",
  "description": "Neuron Network and PSO example",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "parcel",
    "build": "parcel build -d dist-pso --no-source-maps --no-cache --public-url /smart-birds",
    "build-ga": "parcel build -d dist-pso/ga --no-source-maps --no-cache --public-url /smart-birds/ga index2.html",
    "test": "jest"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "jest": "^23.1.0",
    "parcel-bundler": "^1.8.1"
  }
}
